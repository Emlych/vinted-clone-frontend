import React from "react";
import hero from "../assets/hero.jpg";

const Hero = () => {
  return (
    <div className="header__hero">
      <img src={hero} alt="" />
    </div>
  );
};

export default Hero;
